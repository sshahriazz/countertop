// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model User {
  id            String  @id @default(cuid())
  email         String  @unique
  phone         String? @unique
  firstname     String?
  lastname      String?
  password      String
  emailVerified Boolean @default(false)
  is2fa         Boolean @default(false)
  disableAccess Boolean @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// schema.prisma

// Define Role model
model Role {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  createdAt DateTime @default(now())

  // Define RolePermissions relation to represent role's permissions
}

// Define UserRole model to represent user's assigned roles

// Example seed data for resources
// Add your resources (Blog, Comments, Post, Product, etc.) as needed
// Example:
// model Resource {
//   id   Int    @id @default(autoincrement())
//   name String @unique
//   users User[]
// }
